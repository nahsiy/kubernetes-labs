apiVersion: v1
kind: Pod
metadata:
  labels:
    app: web
  name: web
  namespace: lab-01
spec:
  containers:
    - name: nginx
      image: nginx:1.29.1-alpine
      ports:
        - containerPort: 80
